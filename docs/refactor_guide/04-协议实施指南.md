# 简化协议实施指南 v6.0

> **版本：** v6.0
> **日期：** 2026-05-05
> **目标：** 指导各端开发者按照新的简化协议修改代码

---

## 1. 实施总览

### 1.1 修改量估算

| 组件 | 修改文件数 | 主要操作 | 风险等级 |
|------|-----------|---------|---------|
| ESP32 | 6个 | 删除代码为主，增加并发控制 | 中 |
| 服务器 | 3-4个 | 删除+简化 | 低 |
| App | 2-3个 | 简化逻辑+串行化 | 低 |
| STM32 | **0个** | 无需修改 | 无 |

### 1.2 实施顺序

建议按以下顺序实施，每阶段完成后验证基本功能。**各阶段之间必须做集成测试**，确认无回归再进入下一阶段：

1. **第一阶段**：ESP32 — 删除 pending_commands_、esp32_ack、seq_id 相关代码
   - **验证**：用当前服务器代码测试，确认基础语音对话和门锁功能正常（服务器代码未变，应能兼容）
2. **第二阶段**：ESP32 — 简化 HandleSmartLockJsonMessage 和 HandleLockSystemMessage
   - **验证**：用当前服务器代码测试，确认所有门锁JSON消息格式正确
3. **第三阶段**：服务器 — 删除 esp32AckHandler，简化 commandProxyHandler 和 app_connection
   - **验证**：用已修改的ESP32和未修改的App测试（App仍发seq_id，Server应忽略）
4. **第四阶段**：App — 简化 DeviceService.ts
   - **验证**：全链路测试

### 1.3 兼容性说明

- **原始 xiaozhi-esp32 功能不受影响**：hello、tts、stt、llm、mcp、system、alert、local_preview、custom
- **BinaryProtocol2 不受影响**：音频流、视频流、人脸识别图像
- **MQTT+UDP 备选通道不受影响**
- **STM32 固件无需修改**：UART 7字节帧格式不变，只是ESP32端不再等待ACK

---

## 2. ESP32 端修改清单

### 2.1 application.h — 删除数据结构

**文件：** `xiaozhi-esp32/main/application.h`

#### 删除项1：PendingCommandType 枚举（第57-61行）

```cpp
// 删除以下全部：
enum class PendingCommandType {
  IMMEDIATE,
  QUERY,
  LONG_FLOW,
};
```

#### 删除项2：PendingCommand 结构体（第69-80行）

```cpp
// 删除以下全部：
struct PendingCommand {
  std::string seq_id;
  PendingCommandType type;
  std::string category;
  std::string command;
  uint8_t uart_type;
  uint8_t uart_subtype;
  int64_t timestamp_ms;
  bool esp32_ack_sent;
  bool stm32_ack_received;
  int stm32_error_code;
};
```

#### 删除项3：pending_commands_ 成员变量（第193行）

```cpp
// 删除：
std::map<uint8_t, PendingCommand> pending_commands_;
```

#### 删除项4：超时常量（第196-202行）

```cpp
// 删除以下全部：
static constexpr int64_t IMMEDIATE_TIMEOUT_MS = 3000;
static constexpr int64_t QUERY_TIMEOUT_MS = 5000;
static constexpr int64_t LONG_FLOW_TIMEOUT_MS = 60000;
```

#### 删除项5：方法声明

```cpp
// 删除以下方法声明：
PendingCommandType DetermineCommandType(const std::string &category, const std::string &command);
uint8_t GetUartType(const std::string &category, const std::string &command);
int MapStm32ErrorCode(uint8_t stm32_err);
void CleanupPendingCommands();
```

**注意：** `HandleLockSystemMessage` 声明可以保留（如果还需要处理PONG），或一并删除。`HandleLockEvent`、`HandleLockReportMessage`、`HandleLockUserMessage`、`HandleSmartLockJsonMessage` 保留但需要修改实现。

### 2.2 application.cc — 简化处理逻辑

**文件：** `xiaozhi-esp32/main/application.cc`

#### 修改1：HandleSmartLockJsonMessage（约第2079行起）

**当前逻辑（伪代码）：**
```
HandleSmartLockJsonMessage(root, type):
  if type == "lock_control":
    解析 seq_id
    DetermineCommandType()
    创建 PendingCommand
    存入 pending_commands_
    发送 esp32_ack
    发送 UART 命令给 STM32
  if type == "dev_control":
    同上
  if type == "user_mgmt":
    同上
  if type == "face_result":
    直接处理
  if type == "heartbeat_ack":
    直接处理
```

**修改后逻辑（伪代码）：**
```
HandleSmartLockJsonMessage(root, type):
  if type == "lock_control":
    解析 command
    → 调用 SendLockCommand(command)  // 发送UART，等待RPT_UNLOCK
    → 收到结果后发送 ack
  if type == "dev_control":
    解析 target
    → 调用 SendDevCommand(target, params)  // 发送UART，立即ack
  if type == "user_mgmt":
    解析 category + command
    → 调用 SendUserMgmtCommand(...)  // 发送UART，按命令类型处理
  if type == "face_result":
    解析 result + access.granted
    → 如果授权，发送CMD_LOCK开锁
    → 上报 log_report（不发送ack）
  // heartbeat_ack 处理删除
```

**关键变化：**
- 不再解析 seq_id 字段
- 不再调用 DetermineCommandType / GetUartType
- 不再创建 PendingCommand
- 不再发送 esp32_ack
- heartbeat_ack 处理删除

#### 修改2：HandleLockSystemMessage（约第1459行起）

**当前逻辑：** 处理 STM32 发来的 ACK_OK/ACK_ERR/PONG，更新 pending_commands_，决定是否发送最终 ack。

**修改后逻辑：** 此函数**完全删除**。心跳已确定不需要（见文档00 §2.3），CAT 0x00整类移除，STM32不再发送任何CAT=0x00的消息。

同时修改 `HandleLockEvent` 中的消息分发：删除对 `IsSys()`（CAT=0x00）的分支判断。因为STM32不会再发送CAT=0x00的消息，该分支永远不会被触发。

#### 删除项：以下函数全部删除

1. `CleanupPendingCommands()` — 约第2702行
2. `MapStm32ErrorCode()` — 约第2634行
3. `DetermineCommandType()` — 查找并删除
4. `GetUartType()` — 约第2566行

#### 修改3：MainEventLoop 中的 CLOCK_TICK 处理

在 `MAIN_EVENT_CLOCK_TICK` 处理中，删除对 `CleanupPendingCommands()` 的调用。

### 2.3 websocket_protocol.h — 删除方法声明

**文件：** `xiaozhi-esp32/main/protocols/websocket_protocol.h`

#### 删除项：

```cpp
// 删除以下方法声明：
void SendEsp32Ack(const std::string &seq_id);
bool IsDuplicateMsgId(const std::string &msg_id);
void SendHeartbeat();
void SendPasswordReport(const std::string &password);

// 删除成员变量：
std::deque<std::string> msg_id_cache_;  // 防重放缓存
static constexpr size_t MSG_ID_CACHE_SIZE = 100;
```

#### 修改项：SendAck 签名简化

```cpp
// 旧版：
void SendAck(const std::string &seq_id, int code, const std::string &msg);

// 新版（去掉seq_id）：
void SendAck(int code, const std::string &msg);
```

### 2.4 websocket_protocol.cc — 删除方法实现

**文件：** `xiaozhi-esp32/main/protocols/websocket_protocol.cc`

#### 删除以下函数实现：

1. `SendEsp32Ack()` — 约第504行
2. `IsDuplicateMsgId()` — 约第466行
3. `SendHeartbeat()` — 约第608行
4. `SendPasswordReport()` — 约第647行

#### 修改：SendAck 实现

```cpp
// 旧版（参考）：
void WebsocketProtocol::SendAck(const std::string &seq_id, int code, const std::string &msg) {
  cJSON *root = cJSON_CreateObject();
  cJSON_AddStringToObject(root, "type", "ack");
  cJSON_AddStringToObject(root, "seq_id", seq_id.c_str());  // 删除
  cJSON_AddNumberToObject(root, "code", code);
  cJSON_AddStringToObject(root, "msg", msg.c_str());
  // ...
}

// 新版：
void WebsocketProtocol::SendAck(int code, const std::string &msg) {
  cJSON *root = cJSON_CreateObject();
  cJSON_AddStringToObject(root, "type", "ack");
  cJSON_AddNumberToObject(root, "code", code);
  cJSON_AddStringToObject(root, "msg", msg.c_str());
  SendText(root);
  cJSON_Delete(root);
}
```

#### 注意：OnData 中的防重放检查

在 `WebsocketProtocol::OnData()` 中，删除以下逻辑：

```cpp
// 删除：
if (IsDuplicateMsgId(seq_id)) {
  ESP_LOGW(TAG, "重复的 seq_id=%s，已忽略", seq_id.c_str());
  SendAck(seq_id, 9, "重复消息");
  return;
}
```

同时，在 `OnData` 中删除以下调用（通常在防重放检查之后）：
```cpp
// 删除：
AddMsgIdToCache(seq_id);
```

**全局搜索** `AddMsgIdToCache` 的调用点，确认全部删除（目前只有 `OnData` 中一处）。

### 2.5 protocol.h — 删除基类方法声明

**文件：** `xiaozhi-esp32/main/protocols/protocol.h`

**注意：** `SendEsp32Ack` 和 `SendPasswordReport` 不在 `protocol.h` 基类中，它们只在 `websocket_protocol.h` 中声明（见§2.3）。此处只修改基类中的内容。

```cpp
// 删除以下虚函数声明（当前在 protocol.h 中）：
virtual void SendHeartbeat();

// 修改 SendAck 签名（去掉 seq_id 参数）：
// 旧：virtual void SendAck(const std::string &msg_id, int code = 0, const std::string &msg = "OK");
// 新：
virtual void SendAck(int code, const std::string &msg);
```

同时修改 `protocol.cc` 中 `SendAck` 的默认实现（去掉 seq_id 参数），并删除 `SendHeartbeat` 的实现。

**注意：** 其他v5.0方法（`SendStatusReport`、`SendEventReport`、`SendLogReport`、`SendDoorOpenedReport`、`SendUserMgmtResult`）的签名和默认实现保持不变。

### 2.6 application.cc — 删除 SendEsp32Ack 调用点

**文件：** `xiaozhi-esp32/main/application.cc`

**全局搜索** `SendEsp32Ack`，找到所有调用点。典型位置在 `HandleSmartLockJsonMessage` 中每个门锁命令分支的入口处：

```cpp
// 删除以下调用模式（每个命令处理分支的入口处都有）：
protocol_->SendEsp32Ack(seq_id);

// 不需要替换为任何代码，直接删除即可
// 因为用户不等待 esp32_ack，且 SendAck 会在命令执行完成后调用
```

**搜索方法**：在 `application.cc` 中搜索 `SendEsp32Ack` 字符串，确认所有调用点都被删除。编译时会因为函数声明被删除而报错，所以漏删的调用点会被编译器发现。

### 2.7 application.cc — 实现并发控制

**文件：** `xiaozhi-esp32/main/application.cc`

在 `Application` 类中增加忙标志和相关逻辑（具体并发规则见文档02 §4.4）：

```cpp
// application.h 增加成员变量：
bool busy_ = false;
int pending_rpt_unlock_count_ = 0;  // face_result场景：等待中的RPT_UNLOCK个数

// application.cc HandleSmartLockJsonMessage 中的并发控制逻辑：
// 在 lock_control 和 user_mgmt(add/query) 处理分支的开头：
if (busy_) {
    protocol_->SendAck(1, "设备忙，请稍后重试");
    return;
}
busy_ = true;

// 在 RPT_UNLOCK 或超时回调中：
busy_ = false;

// face_result 处理：
if (busy_ && 当前在等待RPT_UNLOCK) {
    pending_rpt_unlock_count_++;  // STM32会多上报一次RPT_UNLOCK
}
// 发送CMD_LOCK（不设busy_，不等待）
```

### 2.8 lock_control/lock_control.cc — 简化命令发送

**文件：** `xiaozhi-esp32/main/lock_control/lock_control.cc`

检查是否有等待 ACK 的逻辑（如 `WaitForAck()` 或类似函数），如有则删除。命令发送改为：

```cpp
// 发送命令后不等待ACK，直接返回
void LockControlService::SendCommand(uint8_t cat, uint8_t type, const std::array<uint8_t, 3> &data) {
  auto frame = LockProtocol::BuildMessage(cat, type, data);
  uart_write_bytes(uart_port_, frame.data(), frame.size());
  // 不等待 ACK，直接返回
}
```

---

## 3. 服务器端修改清单

### 3.1 删除 esp32AckHandler

**文件：** `xiaozhi-server/core/handle/textHandler/esp32AckHandler.py`

**操作：** 删除整个文件。

**同时修改注册代码：** 在消息处理器注册文件中（可能在 `textHandle.py` 或 `textMessageHandlerRegistry.py`），删除 esp32AckHandler 的注册。

### 3.2 简化 ackHandler

**文件：** `xiaozhi-server/core/handle/textHandler/ackHandler.py`

**修改：** 移除 seq_id/msg_id 相关的追踪和验证逻辑。ESP32发来的ack直接转发给App。

**修改后逻辑（伪代码）：**
```python
class AckHandler:
    async def handle(self, connection, msg_json):
        # 不再检查 seq_id/msg_id
        # 直接转发给所有关联的App
        await connection.forward_to_apps(msg_json)
```

### 3.3 简化 commandProxyHandler

**文件：** `xiaozhi-server/core/handle/textHandler/commandProxyHandler.py`

**修改：**

1. **去掉 seq_id 生成**：不再为转发给ESP32的命令生成 msg_id
2. **去掉重试机制**：移除重试逻辑
3. **去掉 server_ack 发送**：App发来命令后不立即回复 server_ack，而是等ESP32的ack转发

**修改后逻辑（伪代码）：**
```python
class CommandProxyHandler:
    async def handle(self, app_connection, msg_json):
        # 1. 验证App已认证
        if not app_connection.authenticated:
            await app_connection.send_error("未认证")
            return
        
        # 2. 检查ESP32在线
        esp32_conn = connection_manager.get_esp32(app_connection.device_id)
        if not esp32_conn:
            await app_connection.send_error("设备不在线")
            return
        
        # 3. 记录操作日志
        await self.log_command(app_connection.app_id, msg_json)
        
        # 4. 原封不动转发给ESP32（不添加seq_id）
        await esp32_conn.send_json(msg_json)
        
        # 5. 不发送 server_ack，等ESP32的ack转发
```

### 3.4 简化 app_connection

**文件：** `xiaozhi-server/core/app_connection.py`

**修改：**

1. 移除 server_ack 发送逻辑（如果有独立的 `send_server_ack()` 方法则删除）
2. 移除 seq_id 验证和防重放检查
3. 移除 `seq_id_cache` 相关代码

**简化 handleTextMessage 中的逻辑（伪代码）：**
```python
async def handleTextMessage(self, msg_json):
    msg_type = msg_json.get("type")
    
    # 认证检查（非hello消息需要先认证）
    if msg_type != "hello" and not self.authenticated:
        await self.send_json({"type": "error", "message": "请先认证"})
        return
    
    # 路由到对应处理器
    # 不再检查 seq_id 重复
    handler = self.handler_registry.get(msg_type)
    if handler:
        await handler.handle(self, msg_json)
```

### 3.5 删除 heartbeatHandler（可选）

**文件：** `xiaozhi-server/core/handle/textHandler/heartbeatHandler.py`

如果ESP32不再发送heartbeat，可以删除或保留为空处理。

### 3.6 删除 queryHandler 中的 WebSocket 查询

**文件：** `xiaozhi-server/core/handle/textHandler/queryHandler.py`

此文件处理App端的WebSocket查询（query类型消息）。如果改为全部使用HTTP API，可删除此文件。或者保留但只处理HTTP API（如果queryHandler同时处理HTTP请求的话，需要区分）。

---

## 4. App 端修改清单

### 4.1 简化 DeviceService.ts

**文件：** `smart_door_lock_pro/services/DeviceService.ts`

#### 修改1：去掉 server_ack 等待

```typescript
// 旧版：发送命令后等待 server_ack
async sendCommand(command: any): Promise<void> {
  const seqId = this.generateSeqId();
  command.seq_id = seqId;
  
  this.ws.send(JSON.stringify(command));
  
  // 等待 server_ack（旧逻辑，删除）
  // const ack = await this.waitForServerAck(seqId, 3000);
  
  // 等待 ack（新逻辑：直接等ESP32的ack）
  return this.waitForAck(seqId, 10000);
}

// 新版：不生成seq_id，不等待server_ack
async sendCommand(command: any): Promise<void> {
  // 不添加 seq_id
  this.ws.send(JSON.stringify(command));
  // 不等待 server_ack，等待 ESP32 的 ack（由 Server 转发）
  // ack 的匹配通过 type 字段和时序判断，或简化为"下一个ack就是当前命令的响应"
}
```

#### 修改2：简化消息处理

```typescript
// 旧版：处理 server_ack 和 ack 两种确认
onMessage(data: string) {
  const msg = JSON.parse(data);
  
  if (msg.type === 'server_ack') {  // 删除此分支
    this.handleServerAck(msg);
  } else if (msg.type === 'ack') {
    this.handleAck(msg);
  } else if (msg.type === 'status_report') {
    this.handleStatusReport(msg);
  }
  // ...
}

// 新版：只处理 ack
onMessage(data: string) {
  const msg = JSON.parse(data);
  
  if (msg.type === 'ack') {
    this.handleAck(msg);
  } else if (msg.type === 'status_report') {
    this.handleStatusReport(msg);
  }
  // ...
}
```

#### 修改3：简化错误处理

```typescript
// 旧版：根据 code 0-10 显示不同错误
// 新版：code 0=成功, 1=失败，显示 msg 即可

handleAck(msg: any) {
  if (msg.code === 0) {
    // 成功
    this.onCommandSuccess();
  } else {
    // 失败，显示 msg
    this.showError(msg.msg || '命令执行失败');
  }
}
```

#### 修改4：去掉超时重传

```typescript
// 旧版：超时后重传（最多3次）
// 新版：超时后直接提示失败，用户手动重试

sendCommand(command: any): Promise<void> {
  return new Promise((resolve, reject) => {
    this.ws.send(JSON.stringify(command));
    
    // 简单超时（10秒），不重传
    const timeout = setTimeout(() => {
      reject(new Error('命令超时，请重试'));
    }, 10000);
    
    this.pendingResolve = resolve;
    this.pendingTimeout = timeout;
  });
}
```

### 4.2 命令串行化

由于ack不带命令标识，App必须确保同一时间只有一个命令在等待ack。除了按钮防抖外，还需要实现命令串行化：

```typescript
// 方案：命令队列
class CommandQueue {
  private queue: Command[] = [];
  private processing = false;

  async enqueue(command: Command): Promise<void> {
    return new Promise((resolve, reject) => {
      this.queue.push({ command, resolve, reject });
      if (!this.processing) {
        this.processNext();
      }
    });
  }

  private async processNext() {
    if (this.queue.length === 0) {
      this.processing = false;
      return;
    }
    this.processing = true;
    const { command, resolve, reject } = this.queue.shift()!;
    try {
      this.ws.send(JSON.stringify(command));
      // 等待ack（10秒超时）
      const ack = await this.waitForAck(10000);
      if (ack.code === 0) resolve();
      else reject(new Error(ack.msg));
    } catch (e) {
      reject(e);
    }
    this.processNext(); // 发送队列中的下一个命令
  }
}
```

**注意：** 如果不需要命令队列的复杂度，也可以简单地在按钮点击后disabled，等ack回来再恢复。但命令队列更健壮——如果用户需要在开锁后自动查状态，可以入队两个命令而不用手动等待。

### 4.3 按钮防抖

由于去掉了协议层的防重放，App端需要在UI层增加按钮防抖：

```typescript
// 所有控制按钮点击后立即disabled 2秒
const [unlockDisabled, setUnlockDisabled] = useState(false);

const handleUnlock = async () => {
  setUnlockDisabled(true);
  try {
    await deviceService.sendCommand({type: 'lock_control', command: 'unlock'});
  } catch (e) {
    // 处理错误
  } finally {
    setTimeout(() => setUnlockDisabled(false), 2000);
  }
};
```

### 4.4 图片加载方式变更

**visit_notification 消息不再包含 Base64 图片数据**（`image` 字段），改为 `image_path`（服务器存储路径）。App收到 visit_notification 后，需要通过 HTTP 下载图片：

```typescript
// 旧版：直接显示内嵌图片（Base64）
const imageBase64 = msg.image;
<Image source={{uri: `data:image/jpeg;base64,${imageBase64}`}} />

// 新版：通过 image_path 构造下载URL
const imageUrl = `${serverBase}/api/doorlock/media/download?path=${encodeURIComponent(msg.image_path)}`;
<Image source={{uri: imageUrl}} />
```

此变更同时影响 **Server 端**：visit_notification 生成时不再从数据库读取图片并Base64编码内嵌，改为直接填入图片存储路径。

---

## 5. STM32 端修改清单

**无需修改。**

STM32固件保持不变：
- 7字节UART帧格式不变
- 所有命令处理逻辑不变
- 上报逻辑不变
- ESP32不再发送PING（0xF0），STM32端原有的PONG回复逻辑保持不动即可（即使不触发也无影响）

---

## 6. 验证检查清单

实施完成后，逐项验证以下功能：

### 6.1 ESP32 基础功能（必须全部通过）

- [ ] 设备上电启动正常
- [ ] WebSocket连接服务器成功
- [ ] hello 握手成功
- [ ] 语音对话（ASR→LLM→TTS）正常
- [ ] OTA固件升级正常
- [ ] 显示屏正常

### 6.2 门锁功能

- [ ] App远程开锁：发送命令 → 锁打开 → 收到ack
- [ ] App远程关锁：发送命令 → 锁关闭 → 收到ack
- [ ] 本地指纹开锁：按压指纹 → 锁打开 → App收到log_report
- [ ] 本地NFC开锁：刷卡 → 锁打开 → App收到log_report
- [ ] 本地密码开锁：输入密码 → 锁打开 → App收到log_report
- [ ] 门铃按下：按下门铃 → App收到event_report(bell)
- [ ] PIR检测：人体经过 → App收到event_report(pir_trigger)
- [ ] App控制蜂鸣器：发送dev_control(beep) → 蜂鸣器响 → 收到ack
- [ ] App查看设备状态：status_report正确显示电量、锁状态

### 6.3 AI功能

- [ ] 人脸识别开锁：门铃/PIR触发 → 拍照上传 → 识别成功 → 开锁
- [ ] 到访通知：人脸识别完成 → App收到visit_notification
- [ ] 访客意图识别：无权限访客对话 → App收到visitor_intent_notification

### 6.4 监控功能

- [ ] 启动监控：App发送start_monitor → 收到视频流
- [ ] 停止监控：App发送stop_monitor → 视频流停止
- [ ] 监控对讲：App PCM音频 ↔ ESP32 OPUS音频正常

### 6.5 用户管理

- [ ] 录入指纹：App发命令 → 设备语音提示按手指 → 录入成功 → 收到user_mgmt_result
- [ ] 删除指纹：App发命令 → 删除成功 → 收到ack
- [ ] 设置密码：App发命令 → 设置成功 → 收到ack

### 6.6 协议级别验证（必须全部通过）

以下验证关注协议格式和代码清理的正确性，而非功能行为：

- [ ] **无 esp32_ack**：抓包确认ESP32不再发送 `type: "esp32_ack"` 的JSON消息
- [ ] **无 seq_id**：抓包确认所有ESP32发出的JSON消息中不包含 `seq_id` 字段
- [ ] **无 pending_commands_**：编译通过，`pending_commands_` 相关符号（`PendingCommand`、`PendingCommandType`、`CleanupPendingCommands`、`MapStm32ErrorCode`、`DetermineCommandType`、`GetUartType`）在代码中无残留引用
- [ ] **无防重放**：抓包确认ESP32不再发送 `code:9` 的重复消息拒绝响应
- [ ] **重复命令可用**：2秒内连续发送两次相同的 `dev_control`(beep) 命令，确认两次都收到 `code:0` 的ack（不被防重放机制拒绝）
- [ ] **无 heartbeat**：抓包确认ESP32不发送 `type: "heartbeat"` 消息
- [ ] **ack格式正确**：所有ack消息格式为 `{"type":"ack","code":0,"msg":"ok"}` 或 `{"type":"ack","code":1,"msg":"<描述>"}`，不包含 `seq_id` 字段
- [ ] **UART校验和正确**：用逻辑分析仪或示波器捕获UART波形，验证校验和计算公式 `(CAT+TYPE+D0+D1+D2)&0xFF` 在代码修改后仍然正确
- [ ] **App串行约束生效**：App端确认两个连续按钮点击不会导致两个命令同时pending（按钮在pending期间disabled）

协议级别的验证建议用简单的 Python 脚本抓 WebSocket 消息来完成，示例：

```python
# 简单的 WebSocket 消息格式验证脚本
import websocket
import json

def on_message(ws, message):
    msg = json.loads(message)
    # 验证：不应有 seq_id
    if 'seq_id' in msg:
        print(f"FAIL: seq_id found in message: {msg['type']}")
    # 验证：不应有 esp32_ack type
    if msg.get('type') == 'esp32_ack':
        print(f"FAIL: esp32_ack still being sent")
    # 验证：ack 格式
    if msg.get('type') == 'ack':
        assert 'code' in msg, "ack missing code"
        assert 'msg' in msg, "ack missing msg"
        assert msg['code'] in (0, 1), f"unexpected code: {msg['code']}"
        print(f"OK: ack code={msg['code']}, msg={msg['msg']}")

ws = websocket.WebSocketApp("ws://server:8000/ws",
    on_message=on_message)
ws.run_forever()
```

---

## 7. 回滚方案

如果简化后出现无法解决的问题：

1. **ESP32端**：`git checkout` 恢复 `application.h`、`application.cc`、`websocket_protocol.h`、`websocket_protocol.cc`、`protocol.h`、`protocol.cc`、`lock_control/lock_control.cc`
2. **服务器端**：`git checkout` 恢复 `esp32AckHandler.py`、`ackHandler.py`、`commandProxyHandler.py`、`app_connection.py`
3. **App端**：`git checkout` 恢复 `DeviceService.ts` 及相关UI组件（按钮disabled逻辑）

回滚不影响STM32固件（无需修改）。

---

## 8. 文档版本

| 版本 | 日期 | 说明 |
|------|------|------|
| ... | ... | v1.0–v5.2 历史版本 |
| v6.0 | 2026-05-05 | 初始简化版实施指南 |

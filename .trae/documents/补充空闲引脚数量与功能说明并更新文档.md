## 目标
- 统计 bread-compact-wifi-s3cam 板型的空闲引脚数量，分组列出用途与约束，形成表格化说明。
- 更新引脚对照文档与硬件接线文档，明确“空闲引脚数”“推荐用途”“慎用原因”。

## 变更
1. 修订 `docs/ESP32S3_面包板S3CAM引脚对照.md`：新增“空闲引脚与功能”章节，包含：
   - 可用空闲：`GPIO3, GPIO22–37, GPIO43, GPIO44, GPIO46`（总计 20 个）
   - 预留未占用：`GPIO14`（LAMP）
   - 为各分组给出典型功能（I2C/SPI2/LEDC/PWM/GPIO中断、UART0），加“以模组规格与 pinout 图为准”的声明。
   - 单独标注 `GPIO43/44`（UART0）与 `GPIO46/3`（strapping）注意事项。
2. 修订 `docs/硬件接线-面包板摄像头.md`：在“剩余引脚与建议用途”补充“空闲引脚总数”与分组用途要点。

## 验证
- 以 `config.h` 和 `compact_wifi_board_s3cam.cc` 为基准，确保已占用列表准确，空闲集合为剩余集合。
- 保留声明：具体 ADC/Touch/LEDC 能力以 `docs/ESP32S3_Pinout.png` 与模组规格为准。

## 交付
- 两处文档更新并生效，用户可直接查看空闲引脚数量与用途说明。
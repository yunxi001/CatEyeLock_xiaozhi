## 目标
- 面向 `main/boards/bread-compact-wifi-s3cam/`，修订现有硬件接线文档，增加“已用/未用/慎用”引脚与推荐用途。
- 新增一份对照标注文档：逐脚列出占用情况、用途与关键约束，并给出源码定位行号与能力提示（参考 `ESP32S3_Pinout.png`）。

## 数据来源与范围
- 仅以本板目录代码为准：`config.h` 与 `compact_wifi_board_s3cam.cc`。
- 不对外部模组专用脚（Flash/PSRAM）做绝对占用断言，统一以“可能占用/需查模组规格”提示。

## 修订与新增
1. 更新 `docs/硬件接线-面包板摄像头.md`
   - 补充“剩余引脚与推荐用途”栏目：
     - 已占用清单（音频/摄像头/显示/按钮/LED/LAMP）。
     - 未用候选与建议用途（I2C/SPI/PWM/输入中断）。
     - 慎用与保留（USB 19/20、JTAG 39–42、strapping 0/3/45/46、UART0 43/44）。
     - 统一加入源码行号定位。
2. 新增 `docs/ESP32S3_面包板S3CAM引脚对照.md`
   - 逐脚列出状态：已用/未用/慎用/预留，注明用途与代码路径。
   - 提示能力：USB/JTAG/UART0/ADC/Touch/LEDC 等，能力说明以官方引脚图为依据，并加“以模组规格为准”声明。
3.（可选）在 `README.md` “开发者文档”增加新对照文档链接。

## 验证
- 交叉核对 `config.h` 与初始化代码。
- 将“USB 19/20 被显示占用”“默认 SIMPLEX I2S”“SCCB 在 4/5”写入显著位置。

## 交付
- 两个文档更新/新增；如需要，我再补充基于 `docx` 的教程引脚列对照。